import { ContainerRefDirective } from './container-ref.directive';

describe('ContainerRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerRefDirective({} as any);
    expect(directive).toBeTruthy();
  });
});

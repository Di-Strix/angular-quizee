import { SettingCardComponent } from './setting-card.component';

describe('SettingCardComponent', () => {
  let component: SettingCardComponent;

  beforeEach(() => {
    component = new SettingCardComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

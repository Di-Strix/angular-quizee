import { ResultsScreenComponent } from './results-screen.component';

describe('ResultsScreenComponent', () => {
  let component: ResultsScreenComponent;

  beforeEach(async () => {
    component = new ResultsScreenComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

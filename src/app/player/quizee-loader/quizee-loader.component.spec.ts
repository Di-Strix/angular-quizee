import { QuizeeLoaderComponent } from './quizee-loader.component';

describe('QuizeeLoaderComponent', () => {
  let component: QuizeeLoaderComponent;

  beforeEach(async () => {
    component = new QuizeeLoaderComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

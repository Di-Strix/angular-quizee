import { ResultsLoaderComponent } from './results-loader.component';

describe('ResultsLoaderComponent', () => {
  let component: ResultsLoaderComponent;

  beforeEach(async () => {
    component = new ResultsLoaderComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

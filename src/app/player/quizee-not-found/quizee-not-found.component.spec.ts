import { QuizeeNotFoundComponent } from './quizee-not-found.component';

describe('QuizeeNotFoundComponent', () => {
  let component: QuizeeNotFoundComponent;

  beforeEach(async () => {
    component = new QuizeeNotFoundComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
